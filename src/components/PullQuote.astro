<blockquote class="quote"><slot /></blockquote>

<style>
  .quote {
    padding-left: var(--pad-inset);
    font: var(--t-callout);
    letter-spacing: var(--track-callout);
    color: var(--ink);
    border-left: 2px solid var(--accent);
    text-wrap: pretty;
  }
</style>
